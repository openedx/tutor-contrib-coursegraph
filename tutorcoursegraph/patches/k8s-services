---
apiVersion: v1
kind: Service
metadata:
  name: coursegraph
spec:
  type: NodePort
  ports:
    - name: http
      port: {{ COURSEGRAPH_NEO4J_HTTP_PORT }}
      protocol: TCP
    - name: bolt
      port: {{ COURSEGRAPH_NEO4J_BOLT_PORT }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: coursegraph
