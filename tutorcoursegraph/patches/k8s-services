---
apiVersion: v1
kind: Service
metadata:
  name: coursegraph
spec:
  type: NodePort
  ports:
    - port: {{ COURSEGRAPH_NEO4J_HTTP_PORT }}
      protocol: TCP
    - port: {{ COURSEGRAPH_NEO4J_BOLT_PORT }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: coursegraph
